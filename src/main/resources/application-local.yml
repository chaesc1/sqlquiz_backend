spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sqlquiz
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    sqlquiz: DEBUG
    org.hibernate.SQL: DEBUG
